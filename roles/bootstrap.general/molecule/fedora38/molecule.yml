---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: fedora_38_testing
    image: ansiblinux-fedora:38
    pre_build_image: true
    privileged: true            # Needed for running SystemD with no permission errors

provisioner:
  name: ansible

verifier:
  name: ansible
