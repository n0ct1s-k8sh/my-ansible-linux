---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: ansiblinux_fedora_38
    image: ansiblinux-fedora:38
    pre_build_image: true
    privileged: true            # Needed for running SystemD with no permission errors

provisioner:
  name: ansible

verifier:
  name: ansible
